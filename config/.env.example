# Theatre Project Environment Variables Example
# Copy this file to .env and update with your values

# Jellyfin Configuration
JELLYFIN_URL=http://localhost:8096

# Caddy Reverse Proxy Configuration
# Domain name for HTTPS access (e.g., movietheatre.duckdns.org)
DOMAIN_NAME=movietheatre.duckdns.org
# DuckDNS token is now stored as a Docker secret
# See config/secrets/duckdns_token.txt.example for setup instructions

# gocryptfs Configuration (if applicable)
# GOCRYPTFS_PASSWORD=your-encryption-password

# Google Cloud Configuration (future use)
# GCP_PROJECT_ID=your-project-id
# GCP_REGION=us-central1
